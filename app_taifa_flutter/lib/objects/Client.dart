import 'package:flutter/material.dart';

class Client {
  String name;
  Color color;
  DateTime lastUpdated;

  Client({
    required this.name,
    required this.color,
    required this.lastUpdated,
  });
}
