import 'package:flutter/material.dart';

class Client {
  String name;
  Color color;
  DateTime updatedDate;

  Client({
    required this.name,
    required this.color,
    required this.updatedDate,
  });
}
